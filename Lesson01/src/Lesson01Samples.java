import java.util.Scanner;

public class Lesson01Samples {
    public static void main(String[] args) {
        // Use this to test FibPrinter and Lesson01Samples

        HelloWorldPrinter printer = new HelloWorldPrinter();
        printer.helloWorld();

        // Uncomment to test fibPrinter
        // FibPrinter printer = new FibPrinter();
        // printer.askForNumberAtPosition();
    }
}
